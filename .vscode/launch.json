{
    "configurations": [
        {
            "type": "debugpy",
            "request": "launch",
            "name": "Launch solve.py",
            "program": "${workspaceFolder}/solve.py",
            "args": [
                "${input:dayNumber}"
            ]
        }
    ],
    "inputs": [
        {
            "type": "promptString",
            "id": "dayNumber",
            "description": "Enter the day number to run"
        }
    ]
}
